server:
  port: 2000

spring:
  application:
    name: destiny-chip-mysql
  datasource:
    name: mydb
    username: root
    password: 12138
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/destiny_chip?serverTimezone=Asia/Shanghai

mybatis:
  mapper-locations: classpath:./mybatis/mapper/*Mapper.xml

logging:
  level:
    com.fatehole.destinychip.mapper: debug
    com.fatehole.destinychip.test: debug

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka
